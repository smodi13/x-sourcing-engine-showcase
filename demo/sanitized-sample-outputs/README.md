# Sanitized sample outputs

These JSON files are the sanitized, public demo data produced by `scripts/build_sanitized_demo_data.py` from a completed private sourcing run. They are identical to the files served from `public/demo-data/` and are included here as a stable reference. They contain no credentials, no raw Post text, and no personal identity fields.
